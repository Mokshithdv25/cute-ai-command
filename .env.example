# Copy to `.env.local` and fill from InsForge: Dashboard → Project → API keys,
# or from `.insforge/project.json` after `npx @insforge/cli link` (do not commit that file).

VITE_INSFORGE_BASE_URL=https://YOUR_APPKEY.us-east.insforge.app
VITE_INSFORGE_ANON_KEY=your_project_api_key
